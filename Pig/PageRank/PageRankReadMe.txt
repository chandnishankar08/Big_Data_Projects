Input - inputProcessed.txt 
inputProcessed.txt was formed by precoss.py to convert the links to a int id so that GraphX can use it.
Output given by te GraphX can be mapped against the Mapping.txt which lists the Link and Id mapped to it.